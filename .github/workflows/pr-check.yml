name: PR check (compile gate)

on:
  pull_request:
    paths:
      - '**/*.swift'
      - '**/*.xcodeproj/**'
      - '**/*.pbxproj'
      - 'project.yml'
      - 'Package.swift'
      - 'Package.resolved'
      - '.github/workflows/pr-check.yml'
      - '.github/ci/adversarial-review.py'
      - '.github/repo-meta.yml'
  push:
    branches-ignore: [main]
    paths:
      - '**/*.swift'
      - '**/*.xcodeproj/**'
      - '**/*.pbxproj'
      - 'project.yml'
      - 'Package.swift'
      - 'Package.resolved'
      - '.github/workflows/pr-check.yml'
      - '.github/ci/adversarial-review.py'
      - '.github/repo-meta.yml'

# head_ref is set on pull_request, empty on push; ref_name is the branch name on both.
# Using ref_name (not ref) makes the push + pull_request runs for one branch share a
# group so cancel-in-progress collapses the duplicate instead of double-running.
concurrency:
  group: pr-check-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  build:
    name: Compile (iOS Simulator, no signing)
    runs-on: [self-hosted, macOS, ios-build]
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Read repo-meta.yml
        id: meta
        run: |
          set -euo pipefail
          if [ ! -f .github/repo-meta.yml ]; then
            echo "::error::.github/repo-meta.yml missing — required by verification framework"
            exit 1
          fi
          SCHEME=$(awk -F': *' '/^scheme:/ {print $2; exit}' .github/repo-meta.yml | tr -d '"' | tr -d "'")
          PROJECT=$(awk -F': *' '/^project:/ {print $2; exit}' .github/repo-meta.yml | tr -d '"' | tr -d "'")
          if [ -z "$SCHEME" ] || [ -z "$PROJECT" ]; then
            echo "::error::repo-meta.yml must define both scheme and project"
            cat .github/repo-meta.yml
            exit 1
          fi
          echo "scheme=$SCHEME"   >> "$GITHUB_OUTPUT"
          echo "project=$PROJECT" >> "$GITHUB_OUTPUT"
          echo "✅ scheme=$SCHEME project=$PROJECT"

      - name: Regenerate Xcode project (xcodegen if present)
        run: |
          XCODEGEN_BIN=$(command -v xcodegen 2>/dev/null || echo /usr/local/bin/xcodegen)
          if [ -f project.yml ] && [ -x "$XCODEGEN_BIN" ]; then
            "$XCODEGEN_BIN" generate 2>&1 | tail -10
          else
            echo "no xcodegen / no project.yml — skipping regen"
          fi

      - name: Resolve SPM dependencies
        run: |
          xcodebuild -scheme "${{ steps.meta.outputs.scheme }}" \
                     -project "${{ steps.meta.outputs.project }}" \
                     -derivedDataPath "${{ runner.temp }}/dd-${{ github.run_id }}-${{ github.run_attempt }}" \
                     -resolvePackageDependencies 2>&1 | tail -10

      - name: Compile (Debug, simulator, no signing)
        run: |
          set -o pipefail
          xcodebuild build \
            -scheme "${{ steps.meta.outputs.scheme }}" \
            -project "${{ steps.meta.outputs.project }}" \
            -derivedDataPath "${{ runner.temp }}/dd-${{ github.run_id }}-${{ github.run_attempt }}" \
            -destination "generic/platform=iOS Simulator" \
            -configuration Debug \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            2>&1 | grep -E ": (error|warning):" | head -100

  adversarial-review:
    name: Adversarial review (5-axis)
    runs-on: [self-hosted, macOS, ios-build, ARM64]   # hephaestus — the authenticated claude CLI host
    timeout-minutes: 25
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout PR
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Mint autofix[bot] token (separate reviewer identity)
        id: apptoken
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.AUTOFIX_APP_ID }}
          private-key: ${{ secrets.AUTOFIX_APP_PRIVATE_KEY }}

      - name: Compute PR diff (merge-base vs default branch, same on push + PR)
        run: |
          BASE="${{ github.base_ref }}"
          [ -z "$BASE" ] && BASE="${{ github.event.repository.default_branch }}"
          [ -z "$BASE" ] && BASE=main
          git fetch origin "$BASE" --quiet || true
          MB=$(git merge-base "origin/$BASE" HEAD 2>/dev/null || echo "")
          [ -z "$MB" ] && MB=$(git rev-parse HEAD~1 2>/dev/null || echo "")
          if [ -n "$MB" ]; then git diff "$MB" HEAD > pr.diff; else git diff HEAD > pr.diff; fi
          echo "diff lines: $(wc -l < pr.diff)"

      - name: Run 5-axis adversarial review (claude -p)
        id: review
        run: |
          export PATH="$PATH:$HOME/.local/bin:/opt/homebrew/bin"
          run_review() { python3 .github/ci/adversarial-review.py --diff pr.diff --out advrev.md --json advrev.json; }
          set +e
          run_review; rc=$?
          if [ "$rc" = "2" ]; then
            echo "review incomplete (rc=2, reviewer offline) — retrying once…"
            run_review; rc=$?
          fi
          echo "rc=$rc" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Post/update review comment (autofix[bot], upsert)
        if: ${{ github.event_name == 'pull_request' }}
        env:
          GH_TOKEN: ${{ steps.apptoken.outputs.token }}
          PR: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
        run: |
          { echo '<!-- adversarial-review -->'; cat advrev.md; } > body.md
          CID=$(gh api "repos/$REPO/issues/$PR/comments" --paginate \
                --jq 'map(select(.body|test("adversarial-review"))) | last | .id' 2>/dev/null)
          if [ -n "$CID" ] && [ "$CID" != "null" ]; then
            gh api -X PATCH "repos/$REPO/issues/comments/$CID" -F body=@body.md >/dev/null && echo "updated comment $CID"
          else
            gh pr comment "$PR" --body-file body.md && echo "posted new comment"
          fi

      - name: Gate on high-severity confirmed findings
        run: |
          rc="${{ steps.review.outputs.rc }}"
          if [ "$rc" = "1" ]; then
            echo "::error::Adversarial review found high-severity CONFIRMED finding(s) — see the PR comment."
            exit 1
          fi
          if [ "$rc" = "2" ]; then
            echo "::warning::Adversarial review INCOMPLETE after retry (reviewer offline) — failing OPEN per policy."
          fi
          echo "Gate passed (rc=$rc)."
